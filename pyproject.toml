# Metadata only — there is no build backend and nothing here is published to
# PyPI. The file exists so the Python floor is declared once, in the standard
# place, instead of living in prose. install.sh parses requires-python from
# here; README.md and AGENTS.md quote it.
[project]
name = "sitemap-parser"
version = "0.1.0"
description = "Claude Code skill wrapping ultimate-sitemap-parser to extract a website's sitemap to CSV/TXT."
requires-python = ">=3.10"
